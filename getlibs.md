BREW
----

```sh
   brew install nodejs
   brew install npm
   brew install graphicsmagick
   brew install s3cmd
   brew install multitail
   brew install htop
   brew install boost
```


LUA
---



```sh
   luarocks install lua-cjson
   luarocks install luasocket
   luarocks install image
   luarocks install ffmpeg
   luarocks install imgraph
   luarocks install graphicsmagick
   luarocks install nnx
   luarocks install curl
   luarocks install torchffi
   luarocks install persist
   luarocks install async
   luarocks install md5
   luarocks install gfx.js
   luarocks install manifold
   luarocks install utf8
   luarocks install buffer
   luarocks install redis-async
   luarocks install redis-queue
   luarocks install lbase64
   luarocks install underscore
   luarocks install thmap
```

---- https://github.com/torch/ezinstall


WEB
---

```sh
sudo gem update --system
sudo gem install sass-globbing
sudo npm install http-server -g
npm install jade --global
```
